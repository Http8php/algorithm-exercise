/*
const int N = 1e6 + 10;
int vis[N];
vector<int>p;
void init()
{
    for (int i = 2; i * i <= n; i++)
    {
        if (!vis[i])
        {
            p.push_back(i);
            for (int j = i * i; j <= n; j += i)
            {
                if (!vis[j]) vis[j] = 1;
            }
        }
    }
}
*/
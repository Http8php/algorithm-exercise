/*
for (int i = 0; i < n; i++)
{
    int l = i - 1, r = i + 1;
    while (l >= 0 && r < n && s[l] == s[r])
    {
        l--, r++;
    }
    l = i, r = i + 1;
    while (l >= 0 && r < n && s[l] == s[r])
    {
        l--, r++;
    }
}
*/